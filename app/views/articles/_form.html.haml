= form_for @article do |f|
  - if @article.errors.any?
    %ul
      - @article.errors.full_messages.each do |m|
        %li= m

  %p
    = f.label :title
    = f.text_field :title
  %p
    = f.label :content
    = f.text_area :content

  = f.submit
